<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_implicit_intens"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:text="@string/ui_implicit_intents"
        android:textSize="@dimen/text_size_huge"
        android:textColor="@color/colorBlack"
        android:padding="@dimen/material_margin_first_tier"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="@dimen/text_size_big" />

    <Spinner
        android:id="@+id/spinnerImplicitIntents"
        android:textAlignment="center"
        android:scrollbarSize="@dimen/material_margin_first_tier"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/implicit_intents">
    </Spinner>

    <Button
        style="@style/PrimaryButton"
        android:id="@+id/buttonStartImplicitIntents"
        android:text="@string/ui_start_implicit_intents"
        android:layout_gravity="bottom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>